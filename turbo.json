{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["public/bundle/bundle.js"]
        },
        "dev": {
            "cache": false,
            "persistent": true
        }
    }
}