{
  "name": "@tsx/browser-infra",
  "version": "1.0.0",
  "type": "module",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "build:bundle": "esbuild index.ts --bundle --outfile=../../public/bundle/browser_infra.js --format=iife --target=esnext --sourcemap"
  },
  "dependencies": {
    "@tsx/types": "workspace:*",
    "@tsx/browser-foundation": "workspace:*"
  },
  "devDependencies": {}
}